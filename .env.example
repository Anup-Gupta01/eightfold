# Environment Variables — Candidate Data Transformation Pipeline

NODE_ENV=development
PORT=3000
HOST=0.0.0.0

SAMPLE_DATA_DIR=./sample-data
OUTPUT_DIR=./output

# Optional: uncomment and set if the pipeline fetches external candidate data
# EXTERNAL_API_BASE_URL=https://api.example.com
# EXTERNAL_API_TIMEOUT_MS=10000

LOG_LEVEL=info
